<?php
//****************************************************************************************
//Generated by Cobalt, a rapid application development framework. http://cobalt.jvroig.com
//Cobalt developed by JV Roig (jvroig@jvroig.com)
//****************************************************************************************
require 'path.php';
init_cobalt('View otevaluationratings');

if(isset($_GET['period']) && isset($_GET['stud_id']) && isset($_GET['item_id']) && isset($_GET['target_id']) && isset($_GET['subject_code']) && isset($_GET['section']))
{
    $period = urldecode($_GET['period']);
    $stud_id = urldecode($_GET['stud_id']);
    $item_id = urldecode($_GET['item_id']);
    $target_id = urldecode($_GET['target_id']);
    $subject_code = urldecode($_GET['subject_code']);
    $section = urldecode($_GET['section']);
    require 'form_data_otevaluationratings.php';
}

if(xsrf_guard())
{
    init_var($_POST['btn_back']);

    if($_POST['btn_back'])
    {
        log_action('Pressed cancel button');
        require 'components/query_string_standard.php';
        redirect("listview_otevaluationratings.php?$query_string");
    }
}
require 'subclasses/otevaluationratings_html.php';
$html = new otevaluationratings_html;
$html->draw_header('Detail View: Otevaluationratings', $message, $message_type);
$html->draw_listview_referrer_info($filter_field_used, $filter_used, $page_from, $filter_sort_asc, $filter_sort_desc);
$html->detail_view = TRUE;
$html->draw_controls('view');

$html->draw_footer();